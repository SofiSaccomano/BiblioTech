@model Biblioteca.Models.LivroListViewModel
@{
    ViewData["Title"] = "Cadastro de Livros";
    var mensagemErro = Model.ErrorMessage;
}

<div id="listaLivros" class=" text-dark container tabela-reservas p-5">

    @if (!string.IsNullOrEmpty(mensagemErro))
    {
        <div class="alert alert-danger text-center alert-dismissible w-50 m-auto mb-3 fs-4 fade show " role="alert">
            @Html.Raw(mensagemErro)
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <h1 class="text-center text-dark mb-3">Cadastro de Livros</h1>

    <div class="row align-items-start gap-3 mb-4">
        <!-- Botão Adicionar -->
        <div class="col-auto">
            <a asp-action="Create" class="btn btn-outline-light px-4 d-flex align-items-center" style="height: 54px; background-color:#52130C;">
                <i class="bi bi-plus-circle"></i> Adicionar
            </a>
        </div>

        <!-- Campo de Busca -->
        <div class="col">
            <form asp-action="Index" method="get">
                <div class="input-group input-group-lg">
                    <input type="text" name="searchTerm" value="@Context.Request.Query["searchTerm"]" class="form-control" placeholder="Buscar por gênero..." />
                    <button type="submit" class="btn btn-outline-light" style="background-color:#52130C">
                        <i class="bi bi-search"></i> Buscar
                    </button>
                </div>
            </form>
        </div>
    </div>



    <table class="table tabela-reservas text-dark table-striped">
        <thead>
            <tr>
                <th>Capa</th>
                <th>
                    <a class="text-dark text-decoration-none" asp-action="Index" asp-route-sortOrder="@ViewData["TituloSortParm"]" asp-route-searchTerm="@Model.SearchTerm">
                        Título
                        @if (Model.SortOrder == "titulo_asc")
                        {
                            <i class="bi bi-caret-up-fill"></i>
                        }
                        else if (Model.SortOrder == "titulo_desc")
                        {
                            <i class="bi bi-caret-down-fill"></i>
                        }
                    </a>
                </th>
                <th>
                    <a class="text-dark text-decoration-none" asp-action="Index" asp-route-sortOrder="@ViewData["AutorSortParm"]" asp-route-searchTerm="@Model.SearchTerm">
                        Autor
                        @if (Model.SortOrder == "autor_asc")
                        {
                            <i class="bi bi-caret-up-fill"></i>
                        }
                        else if (Model.SortOrder == "autor_desc")
                        {
                            <i class="bi bi-caret-down-fill"></i>
                        }
                    </a>
                </th>
                <th>
                    <a class="text-dark text-decoration-none" asp-action="Index" asp-route-sortOrder="@ViewData["EditoraSortParm"]" asp-route-searchTerm="@Model.SearchTerm">
                        Editora
                        @if (Model.SortOrder == "editora_asc")
                        {
                            <i class="bi bi-caret-up-fill"></i>
                        }
                        else if (Model.SortOrder == "editora_desc")
                        {
                            <i class="bi bi-caret-down-fill"></i>
                        }
                    </a>
                </th>
                <th>
                    <a class="text-dark text-decoration-none" asp-action="Index" asp-route-sortOrder="@ViewData["GeneroSortParm"]" asp-route-searchTerm="@Model.SearchTerm">
                        Gênero
                        @if (Model.SortOrder == "genero_asc")
                        {
                            <i class="bi bi-caret-up-fill"></i>
                        }
                        else if (Model.SortOrder == "genero_desc")
                        {
                            <i class="bi bi-caret-down-fill"></i>
                        }
                    </a>
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model.TotalPages > 1)
            {
                <nav class="mb-3">
                    <ul class="pagination justify-content-center">
                        <li class="page-item @(Model.PageIndex == 1 ? "disabled" : "")">
                            <a class="page-link bg-dark text-dark border border-light"
                               asp-action="Index"
                               asp-route-page="@(Model.PageIndex - 1)"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-sortOrder="@Model.SortOrder">
                                Anterior
                            </a>
                        </li>
                        @for (int i = 1; i <= Model.TotalPages; i++)
                        {
                            <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                                <a class="page-link bg-dark text-dark border border-light @(i == Model.PageIndex ? "fw-bold" : "")"
                                   asp-action="Index"
                                   asp-route-page="@i"
                                   asp-route-searchTerm="@Model.SearchTerm"
                                   asp-route-sortOrder="@Model.SortOrder">
                                    @i
                                </a>
                            </li>
                        }
                        <li class="page-item @(Model.PageIndex == Model.TotalPages ? "disabled" : "")">
                            <a class="page-link bg-dark text-dark border border-light"
                               asp-action="Index"
                               asp-route-page="@(Model.PageIndex + 1)"
                               asp-route-searchTerm="@Model.SearchTerm"
                               asp-route-sortOrder="@Model.SortOrder">
                                Próxima
                            </a>
                        </li>
                    </ul>
                </nav>
            }
            @foreach (var item in Model.Livros)
            {
                <tr>
                    <td>
                        @if (!string.IsNullOrEmpty(item.UrlCapa))
                        {
                            <img src="@Url.Action("BuscarCapa", "Livros", new { id = item.LivroId })"
                                 class="img-thumbnail"
                                 style="width: 50px; height: 75px; object-fit: cover;" />
                        }
                        else
                        {
                            <img src="~/img/sem-imagem.jpg" alt="Sem Capa" class="img-thumbnail" style="width: 50px; height: 75px; object-fit: cover;" />
                        }
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Titulo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Autor)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Editora)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Genero.Nome)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.LivroId" class="text-dark text-decoration-none me-2"><i class="bi bi-pencil-square"></i></a>
                        <a asp-action="Delete" asp-route-id="@item.LivroId" class="text-dark text-decoration-none me-2"><i class="bi bi-trash3"></i></a>
                        <form asp-controller="Reservas" asp-action="Create" method="post" class="d-inline">
                            <input type="hidden" name="LivroId" value="@item.LivroId" />
                            <button type="submit" class="btn text-dark">
                                <i class="bi bi-check-square"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

